<section class="videos-section">
  <div class="videos-container">
    <div class="section-header">
      <div class="section-kicker">{{ section.settings.kicker }}</div>
      <h2 class="section-title">{{ section.settings.title }}</h2>
    </div>

    {% if section.settings.tagline %}
      <p class="videos-tagline">{{ section.settings.tagline }}</p>
    {% endif %}

    <div class="videos-grid">
      {% for block in section.blocks %}
        {% if block.type == 'video' %}
          <div class="video-card" {{ block.shopify_attributes }}>
            {% if block.settings.video_url != blank %}
              <div class="video-wrapper" onclick="playVideo(this, '{{ block.settings.video_url }}')">
                {% if block.settings.thumbnail_image %}
                  <img src="{{ block.settings.thumbnail_image | image_url: width: 600 }}" alt="{{ block.settings.caption }}" class="video-thumbnail">
                {% else %}
                  <div class="video-placeholder"></div>
                {% endif %}
                <div class="video-credit">{{ block.settings.credit }}</div>
                <div class="video-overlay">
                  <div class="play-button">▶</div>
                </div>
                <div class="video-caption">{{ block.settings.caption }}</div>
              </div>
            {% else %}
              <div class="video-placeholder-static">
                <div class="video-credit">{{ block.settings.credit }}</div>
                <div class="video-overlay">
                  <div class="play-button">▶</div>
                </div>
                <div class="video-caption">{{ block.settings.caption }}</div>
              </div>
            {% endif %}
          </div>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</section>

<style>
  .videos-section {
    padding: 80px 5%;
    background: #FFF9F3;  /* Cream-light background like original */
  }

  .videos-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 24px;
  }

  .section-kicker {
    font-size: 12px;
    letter-spacing: 2px;
    color: {{ section.settings.kicker_color }};
    font-weight: 700;
    margin-bottom: 10px;
    text-transform: uppercase;
  }

  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: {{ section.settings.title_size }}px;
    font-weight: 800;
    color: {{ section.settings.title_color }};
    margin: 0;
    letter-spacing: -0.5px;
  }

  .videos-tagline {
    text-align: center;
    font-size: 15px;  /* 15px like original, not 16px */
    color: {{ section.settings.tagline_color }};
    margin-top: -30px;  /* Negative margin-top like original */
    margin-bottom: 50px;
  }

  .videos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 32px;
  }

  .video-card {
    position: relative;
    height: 400px;  /* Fixed height like original */
    border-radius: 12px;  /* 12px like original */
    overflow: hidden;
    cursor: pointer;
    transition: transform 0.3s;
  }

  .video-card:hover {
    transform: scale(1.02);  /* Slight scale on hover like original */
  }

  .video-wrapper {
    position: relative;
    width: 100%;
    height: 100%;
    background: {{ section.settings.placeholder_bg_color }};
    overflow: hidden;
    cursor: pointer;
  }

  .video-thumbnail {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .video-placeholder,
  .video-placeholder-static {
    position: relative;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #C4A57B 0%, #A68B5B 100%);  /* Original gradient colors */
  }

  .video-placeholder-static {
    cursor: pointer;
  }

  .video-credit {
    position: absolute;
    top: 16px;  /* 16px from top like original */
    right: 16px;  /* Right side like original, not left */
    background: rgba(255,255,255,0.9);  /* White/transparent background like original */
    color: #2C2416;  /* Dark text on light background */
    padding: 4px 12px;
    border-radius: 12px;
    font-size: 11px;
    font-weight: 600;
    z-index: 2;
  }

  .video-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.2);
    transition: background 0.3s;
  }

  .video-wrapper:hover .video-overlay,
  .video-placeholder-static:hover .video-overlay {
    background: rgba(0,0,0,0.4);
  }

  .play-button {
    width: 72px;
    height: 72px;
    background: {{ section.settings.play_button_bg }};
    color: {{ section.settings.play_button_color }};
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 28px;
    padding-left: 6px;
    box-shadow: 0 8px 24px rgba(0,0,0,0.3);
    transition: all 0.3s;
  }

  .video-card:hover .play-button {
    transform: scale(1.1);
    box-shadow: 0 8px 24px rgba(0,0,0,0.3);
  }

  .video-caption {
    position: absolute;  /* Position inside card like original */
    bottom: 0;  /* At bottom of card */
    left: 0;
    right: 0;
    padding: 20px;
    font-size: 14px;
    color: white;  /* White text since it's inside the card */
    font-weight: 600;
    z-index: 2;
  }

  /* Embedded video iframe */
  .video-wrapper iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
    border-radius: 16px;
  }

  @media (max-width: 768px) {
    .videos-grid {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .section-title {
      font-size: calc({{ section.settings.title_size }}px * 0.8);
    }
  }
</style>

<script>
  function playVideo(element, videoUrl) {
    if (!videoUrl) return;

    // Create iframe for embedded video
    const iframe = document.createElement('iframe');
    iframe.src = videoUrl + (videoUrl.includes('?') ? '&' : '?') + 'autoplay=1';
    iframe.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture';
    iframe.allowFullscreen = true;

    // Replace thumbnail with iframe
    element.innerHTML = '';
    element.appendChild(iframe);
  }
</script>

{% schema %}
{
  "name": "Customer Videos",
  "settings": [
    {
      "type": "range",
      "id": "section_padding",
      "label": "Section Padding",
      "min": 40,
      "max": 120,
      "step": 10,
      "default": 80,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Section Header"
    },
    {
      "type": "text",
      "id": "kicker",
      "label": "Kicker Text",
      "default": "REAL STORIES"
    },
    {
      "type": "color",
      "id": "kicker_color",
      "label": "Kicker Color",
      "default": "#8B6F47"
    },
    {
      "type": "textarea",
      "id": "title",
      "label": "Title",
      "default": "Real Customers Using CutCraving"
    },
    {
      "type": "range",
      "id": "title_size",
      "label": "Title Size",
      "min": 28,
      "max": 56,
      "step": 2,
      "default": 42,
      "unit": "px"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title Color",
      "default": "#2C2416"
    },
    {
      "type": "text",
      "id": "tagline",
      "label": "Tagline (optional)",
      "default": "100+ customers have shared their CutCraving routine"
    },
    {
      "type": "color",
      "id": "tagline_color",
      "label": "Tagline Color",
      "default": "#6B5D4F"
    },
    {
      "type": "header",
      "content": "Video Style"
    },
    {
      "type": "color",
      "id": "placeholder_bg_color",
      "label": "Placeholder Background",
      "default": "#C4A57B"
    },
    {
      "type": "color",
      "id": "play_button_bg",
      "label": "Play Button Background",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "play_button_color",
      "label": "Play Button Icon Color",
      "default": "#2D5F3F"
    }
  ],
  "blocks": [
    {
      "type": "video",
      "name": "Video",
      "settings": [
        {
          "type": "url",
          "id": "video_url",
          "label": "Video URL (YouTube/Vimeo embed URL)",
          "info": "Example: https://www.youtube.com/embed/VIDEO_ID"
        },
        {
          "type": "image_picker",
          "id": "thumbnail_image",
          "label": "Thumbnail Image (optional)",
          "info": "Shown before video plays. If empty, uses gradient placeholder."
        },
        {
          "type": "text",
          "id": "credit",
          "label": "Credit Text",
          "default": "Shot by real customers"
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption",
          "default": "Real customer using CutCraving"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Customer Videos",
      "blocks": [
        {
          "type": "video",
          "settings": {
            "credit": "Shot by real customers",
            "caption": "Real customer using CutCraving"
          }
        },
        {
          "type": "video",
          "settings": {
            "credit": "Shot by real customers",
            "caption": "Real customer using CutCraving"
          }
        },
        {
          "type": "video",
          "settings": {
            "credit": "Shot by real customers",
            "caption": "Real customer using CutCraving"
          }
        }
      ]
    }
  ]
}
{% endschema %}
