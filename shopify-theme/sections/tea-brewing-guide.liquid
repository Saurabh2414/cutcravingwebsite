{% render 'pdp-animations' %}

<section class="tea-brewing-guide">
  <div class="brewing-container">
    <div class="brewing-header">
      <span class="brewing-badge">☕ {{ section.settings.badge_text }}</span>
      <h2 class="brewing-title animate-fade-up">{{ section.settings.title }}</h2>
      <p class="brewing-subtitle animate-fade-up delay-1">{{ section.settings.subtitle }}</p>
    </div>

    <div class="brewing-steps">
      {% for block in section.blocks %}
        <div class="step-card animate-stagger delay-{{ forloop.index }}" {{ block.shopify_attributes }}>
          <div class="step-image-wrapper">
            <!-- Animated Step Number with SVG Stroke Fill -->
            <div class="step-number-container">
              <svg class="step-number-svg" width="48" height="48" viewBox="0 0 48 48">
                <circle cx="24" cy="24" r="22" fill="#2C2416" stroke="rgba(255,255,255,0.3)" stroke-width="2"/>
                <circle class="step-circle-progress" cx="24" cy="24" r="22" fill="none" stroke="white" stroke-width="3" stroke-dasharray="138.23" stroke-dashoffset="138.23"/>
              </svg>
              <span class="step-number-text">{{ forloop.index }}</span>
            </div>

            {% if block.settings.image %}
              <img
                src="{{ block.settings.image | img_url: '600x600' }}"
                alt="Step {{ forloop.index }}"
                loading="lazy"
                class="step-image">
            {% endif %}

            <!-- COLOR BLOOM: Step 2 Water Animation -->
            {% if forloop.index == 2 %}
              <div class="color-bloom-container">
                <div class="water-clear"></div>
                <div class="water-blue"></div>
              </div>
            {% endif %}

            {% if block.settings.icon %}
              <div class="step-icon-overlay">{{ block.settings.icon }}</div>
            {% endif %}
          </div>

          <div class="step-content">
            <h3 class="step-title">{{ block.settings.step_title }}</h3>
            <p class="step-description">{{ block.settings.step_description }}</p>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</section>

<style>
  .tea-brewing-guide {
    padding: 100px 5%;
    background: #FFF9F3;
  }

  .brewing-container {
    max-width: 1400px;
    margin: 0 auto;
  }

  .brewing-header {
    text-align: center;
    margin-bottom: 70px;
  }

  .brewing-badge {
    display: inline-block;
    padding: 10px 24px;
    background: linear-gradient(135deg, #D4A017 0%, #E8B84D 100%);
    color: white;
    border-radius: 24px;
    font-size: 14px;
    font-weight: 700;
    margin-bottom: 20px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .brewing-title {
    font-family: 'Playfair Display', serif;
    font-size: 42px;
    font-weight: 700;
    color: #2C2416;
    margin: 0 0 16px 0;
    line-height: 1.2;
    letter-spacing: -0.5px;
  }

  .brewing-subtitle {
    font-size: 18px;
    color: #6B5D4F;
    line-height: 1.7;
    max-width: 800px;
    margin: 0 auto;
  }

  .brewing-steps {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 28px;
  }

  .step-card {
    background: white;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,0.06);
    transition: all 0.4s cubic-bezier(0.22, 1, 0.36, 1);
    position: relative;
  }

  .step-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 40px rgba(0,0,0,0.12);
  }

  .step-image-wrapper {
    position: relative;
    width: 100%;
    height: 280px;
    overflow: hidden;
    background: linear-gradient(135deg, #E8F4FF 0%, #F0E8FF 100%);
  }

  /* Animated Step Number */
  .step-number-container {
    position: absolute;
    top: 16px;
    left: 16px;
    width: 48px;
    height: 48px;
    z-index: 2;
  }

  .step-number-svg {
    position: absolute;
    top: 0;
    left: 0;
    filter: drop-shadow(0 4px 12px rgba(44, 36, 22, 0.3));
  }

  .step-circle-progress {
    transform: rotate(-90deg);
    transform-origin: center;
    transition: stroke-dashoffset 1.2s cubic-bezier(0.22, 1, 0.36, 1);
  }

  /* Trigger stroke animation when card is in view */
  .step-card.in-view .step-circle-progress {
    stroke-dashoffset: 0;
  }

  .step-number-text {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    font-size: 20px;
    font-weight: 800;
    z-index: 1;
  }

  .step-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .step-card:hover .step-image {
    transform: scale(1.08);
  }

  .step-icon-overlay {
    position: absolute;
    bottom: 16px;
    right: 16px;
    font-size: 40px;
    filter: drop-shadow(0 2px 8px rgba(0,0,0,0.2));
    z-index: 3;
  }

  /* COLOR BLOOM Animation - Signature Moment */
  .color-bloom-container {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .water-clear,
  .water-blue {
    position: absolute;
    top: 30%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 120px;
    height: 120px;
    border-radius: 50%;
  }

  .water-clear {
    background: radial-gradient(circle, rgba(255,255,255,0.6) 0%, transparent 70%);
    opacity: 1;
  }

  .water-blue {
    background: radial-gradient(circle, rgba(173, 216, 230, 0.9) 0%, rgba(100, 149, 237, 0.6) 40%, transparent 70%);
    opacity: 0;
    transform: translate(-50%, -50%) scale(0.3);
  }

  /* Trigger COLOR BLOOM when step 2 comes into view */
  .step-card:nth-child(2).in-view .water-blue {
    animation: colorBloom 2.5s cubic-bezier(0.22, 1, 0.36, 1) 0.8s forwards;
  }

  .step-card:nth-child(2).in-view .water-clear {
    animation: clearFade 2.5s cubic-bezier(0.22, 1, 0.36, 1) 0.8s forwards;
  }

  @keyframes colorBloom {
    0% {
      opacity: 0;
      transform: translate(-50%, -50%) scale(0.3);
    }
    50% {
      opacity: 1;
    }
    100% {
      opacity: 0.8;
      transform: translate(-50%, -50%) scale(2.5);
    }
  }

  @keyframes clearFade {
    0% {
      opacity: 1;
    }
    100% {
      opacity: 0;
    }
  }

  .step-content {
    padding: 24px;
  }

  .step-title {
    font-size: 18px;
    font-weight: 700;
    color: #2C2416;
    margin: 0 0 10px 0;
    line-height: 1.3;
  }

  .step-description {
    font-size: 14px;
    color: #6B5D4F;
    line-height: 1.6;
    margin: 0;
  }

  /* Step-specific accents */
  .step-card:nth-child(1) .step-image-wrapper {
    background: linear-gradient(135deg, #FFF9E8 0%, #FFF4E0 100%);
  }

  .step-card:nth-child(2) .step-image-wrapper {
    background: linear-gradient(135deg, #E8F4FF 0%, #DDE8FF 100%);
  }

  .step-card:nth-child(3) .step-image-wrapper {
    background: linear-gradient(135deg, #D4A017 0.2, #E8B84D 100%);
  }

  .step-card:nth-child(4) .step-image-wrapper {
    background: linear-gradient(135deg, #E8F4FF 0%, #F0E8FF 100%);
  }

  @media (max-width: 1024px) {
    .brewing-steps {
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }
  }

  @media (max-width: 768px) {
    .tea-brewing-guide {
      padding: 70px 5%;
    }

    .brewing-title {
      font-size: 32px;
    }

    .brewing-subtitle {
      font-size: 16px;
    }

    .brewing-header {
      margin-bottom: 50px;
    }

    .brewing-steps {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .step-image-wrapper {
      height: 240px;
    }

    .step-number-container {
      width: 40px;
      height: 40px;
    }

    .step-number-svg {
      width: 40px;
      height: 40px;
    }

    .step-number-text {
      font-size: 18px;
    }

    .water-clear,
    .water-blue {
      width: 80px;
      height: 80px;
    }

    .step-content {
      padding: 20px;
    }

    .step-title {
      font-size: 16px;
    }

    .step-description {
      font-size: 13px;
    }
  }

  /* Respect reduced motion */
  @media (prefers-reduced-motion: reduce) {
    .step-card:hover {
      transform: none;
    }

    .step-card:hover .step-image {
      transform: none;
    }

    .step-circle-progress {
      transition: none;
    }

    .step-card.in-view .step-circle-progress {
      stroke-dashoffset: 0;
    }

    .step-card:nth-child(2).in-view .water-blue,
    .step-card:nth-child(2).in-view .water-clear {
      animation: none;
    }

    .color-bloom-container {
      display: none;
    }
  }
</style>

{% schema %}
{
  "name": "Tea Brewing Guide",
  "settings": [
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "PERFECT BREW"
    },
    {
      "type": "textarea",
      "id": "title",
      "label": "Section Title",
      "default": "Your Perfect Cup of Blue Tea: A Step-by-Step Guide"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Discover the simple process of brewing the perfect cup of Blue Tea. From selecting the finest tea leaves to adding a touch of lemon, follow these four easy steps to enjoy a refreshing, vibrant cup. Delicious, healthy, and ready in minutes!"
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Brewing Step",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Step Image"
        },
        {
          "type": "html",
          "id": "icon",
          "label": "Icon (emoji or SVG)",
          "default": "🫖"
        },
        {
          "type": "text",
          "id": "step_title",
          "label": "Step Title",
          "default": "Take 1 teaspoon of Blue Tea flowers"
        },
        {
          "type": "textarea",
          "id": "step_description",
          "label": "Step Description",
          "default": "Measure one teaspoon of dried Blue Tea flowers for the perfect flavor balance."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Tea Brewing Guide",
      "blocks": [
        {
          "type": "step",
          "settings": {
            "icon": "🥄",
            "step_title": "Take 1 teaspoon of Blue Tea flowers",
            "step_description": "Measure one teaspoon of dried Blue Tea flowers for the perfect flavor balance."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon": "💧",
            "step_title": "Add Blue Tea to 1 cup of hot water",
            "step_description": "Pour hot water over the Blue Tea flowers in your favorite cup or teapot."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon": "⏱️",
            "step_title": "Steep 5-7 min",
            "step_description": "Let it steep for 5-7 minutes to extract the full flavor and color."
          }
        },
        {
          "type": "step",
          "settings": {
            "icon": "😊",
            "step_title": "Enjoy your cup of Blue Tea",
            "step_description": "Strain and enjoy your delicious, healthy blue tea. Add lemon if desired!"
          }
        }
      ]
    }
  ]
}
{% endschema %}
